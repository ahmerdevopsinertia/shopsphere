Sprint 9 — Next steps

I recommend we build the pipeline incrementally rather than putting everything into one YAML immediately.

Our target:

                git push
                   │
                   ▼
                GitHub
                   │
                   ▼
          ┌─────────────────┐
          │ GitHub Actions  │
          └────────┬────────┘
                   │
          ┌────────▼────────┐
          │ Install npm ci  │
          └────────┬────────┘
                   │
          ┌────────▼────────┐
          │      Lint       │
          └────────┬────────┘
                   │
          ┌────────▼────────┐
          │      Tests      │
          └────────┬────────┘
                   │
          ┌────────▼────────┐
          │     Build       │
          └────────┬────────┘
                   │
          ┌────────▼────────┐
          │  Docker Build   │
          └────────┬────────┘
                   │
          ┌────────▼────────┐
          │ Docker Test Run │
          └────────┬────────┘
                   │
                   ▼
             CI PASSED ✅
Step 3 — Add Lint

Before changing anything, check your backend/package.json for:

"scripts": {
  "lint": "...",
  "test": "...",
  "build": "..."
}

If lint already exists, add this immediately after npm ci:

- name: Run lint
  run: npm run lint

So the current workflow becomes:

name: ShopSphere CI

on:
  push:
    branches:
      - main
      - develop

  pull_request:
    branches:
      - main
      - develop

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: backend

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run lint
        run: npm run lint

      - name: Build application
        run: npm run build

The current GitHub documentation recommends setup-node for selecting Node versions and supports npm dependency caching through the action.

Why I'm changing @v4 → @v6/@v7

Your screenshot's warning is related to the runtime used internally by the Actions themselves, not Node 22 that your application uses.

Current GitHub documentation examples use:

actions/checkout@v6
actions/setup-node@v7

So let's use the current versions rather than carrying the warning into the production pipeline.

Step 4 — Tests

After lint passes, we'll add:

- name: Run tests
  run: npm test

But don't add this yet if your project doesn't currently have a reliable test suite.

Given ShopSphere's current stage, I'd actually prefer:

Lint
 ↓
Unit Tests
 ↓
Build

rather than blindly running tests and potentially discovering that Jest/configuration isn't CI-ready.

Step 5 — Docker build

Once:

npm ci       ✅
lint         ✅
tests        ✅
build        ✅

we'll add Docker.

Since your Docker configuration is already working locally, we'll build the image in GitHub Actions.

Something along the lines of:

- name: Build Docker image
  run: docker build -t shopsphere-backend:ci .

The exact context depends on where your Dockerfile currently lives.

Don't push the image anywhere yet.

First we'll prove:

GitHub runner can successfully build the same Docker image that works on your Mac.

Step 6 — Run the Docker container

Then we'll go one step further:

Docker Build
     ↓
Docker Run
     ↓
Health Check
     ↓
Stop Container

This is particularly important for ShopSphere because your application has:

PostgreSQL
Prisma
JWT configuration
environment validation
seed configuration
Docker networking

We shouldn't consider CI/CD complete just because docker build succeeds.

Step 7 — Docker Registry

After the above works, we'll decide where to publish the image.

For your project, I recommend GitHub Container Registry (GHCR) because your source code is already on GitHub.

Architecture:

GitHub
   │
   ├── CI
   │    ├── npm ci
   │    ├── lint
   │    ├── tests
   │    └── build
   │
   └── CD
        │
        ├── Docker build
        │
        ├── Docker image
        │
        └── GHCR
              │
              ▼
       shopsphere-backend

GitHub officially supports publishing Docker images to ghcr.io using GITHUB_TOKEN, so we don't need to introduce Docker Hub credentials unnecessarily.

Step 8 — Production deployment

This should be the last part, not the first.

We'll eventually have:

Developer
    │
    │ git push
    ▼
GitHub
    │
    ▼
CI
 ├── npm ci
 ├── lint
 ├── test
 ├── build
 └── Docker build
          │
          ▼
       GHCR
          │
          ▼
     Deployment
          │
          ▼
   Production Server

And importantly, your secrets will not be committed into GitHub.

Things like:

DATABASE_URL
JWT_ACCESS_SECRET
JWT_REFRESH_SECRET

will be managed through GitHub Secrets/Environment configuration and eventually your deployment environment.

What I want you to do now

Only do Step 3 first.

Update your ci.yml to:

- name: Checkout repository
  uses: actions/checkout@v6

- name: Setup Node.js
  uses: actions/setup-node@v7
  with:
    node-version: '22'
    cache: npm
    cache-dependency-path: backend/package-lock.json

- name: Install dependencies
  run: npm ci

- name: Run lint
  run: npm run lint

- name: Build application
  run: npm run build

Then:

git add .github/workflows/ci.yml
git commit -m "ci: add lint validation"
git push origin main
Expected result

GitHub Actions should show:

ShopSphere CI
   └── build
       ├── Checkout repository       ✅
       ├── Setup Node.js             ✅
       ├── Install dependencies      ✅
       ├── Run lint                  ✅
       └── Build application         ✅

If lint fails, don't bypass it. Send me the lint error and we'll make the codebase CI-clean first.

Once this is green, we'll do Step 4: automated tests, then Docker. This incremental approach will make Sprint 9 much easier to troubleshoot.

---

Rough work START

----

git tag -a sprint-9-ci-cd -m "Sprint 9: GitHub Actions CI/CD pipeline"
git push origin sprint-9-ci-cd

--

name: Shopsphere CI

on:
  push: 
      branches:
        - main
        - develop

  pull_request:
    branches:
      - main
      - develop
      
jobs:
    build:
      runs-on: ubuntu-latest

      services:
       postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: 12345
          POSTGRES_DB: shopsphere
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U postgres -d shopsphere"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

      defaults:
        run:
          working-directory: backend

      steps:

       - name: Checkout repository
         uses: actions/checkout@v6

       - name: Setup Node.js
         uses: actions/setup-node@v7
         with:
              node-version: 22
              cache: npm
              cache-dependency-path: backend/package-lock.json

       - name: Install dependencies
         run: npm ci

      #  - name: Run lint
      #    run: npm run lint

      #  - name: Run test
      #    run: npm run test

       - name: Build application
         run: npm run build

       - name: Build Docker image
         run: docker build -t shopsphere-backend:ci .

       - name: Run Docker container
         run: |
          docker run -d \
            --name shopsphere-backend-ci \
            -p 3000:3000 \
            -e DATABASE_URL="postgresql://postgres:12345@host.docker.internal:5432/shopsphere" \
            -e JWT_ACCESS_SECRET="ci-access-secret" \
            -e JWT_ACCESS_EXPIRES_IN="1d" \
            -e JWT_REFRESH_SECRET="ci-refresh-secret" \
            -e JWT_REFRESH_EXPIRES_IN="30d" \
            -e RUN_SEED="false" \
            shopsphere-backend:ci

       - name: Wait for application
         run: sleep 10

       - name: Health check
         run: curl --fail http://localhost:3000/health

       - name: Show container logs
         if: always()
         run: docker logs shopsphere-backend-ci

       - name: Stop Docker container
         if: always()
         run: docker stop shopsphere-backend-ci


------

Rough Work Ends

----

Next Step
Excellent! 🎉 That means we now have the first proper CI/CD milestone for ShopSphere completed.

Current ShopSphere pipeline
GitHub main
    │
    ▼
GitHub Actions
    │
    ├── npm ci
    ├── Build NestJS
    ├── Docker Build
    ├── Start PostgreSQL
    ├── Wait for PostgreSQL
    ├── Start ShopSphere
    ├── Prisma migrations
    ├── /health check
    │
    ▼
    CI PASSED
    │
    ▼
GitHub Container Registry
    │
    ├── :latest
    └── :<commit-sha>

So we have successfully completed:

✅ Application build
✅ Dockerization
✅ PostgreSQL integration
✅ Prisma migration during container startup
✅ Container health verification
✅ GitHub Actions CI
✅ GHCR authentication
✅ Docker image publishing
✅ Immutable SHA image tagging
Next step: actual deployment

Now we can move to the next major piece:

GHCR → AWS

Since your original ShopSphere architecture was targeting AWS, I recommend we do not jump directly into Kubernetes/ECS complexity.

For our first deployment, I'd use:

AWS EC2 + Docker
                 GitHub
                    │
                    ▼
             GitHub Actions
                    │
                    ▼
                  GHCR
                    │
                    │ docker pull
                    ▼
             ┌──────────────┐
             │   AWS EC2    │
             │              │
             │ Docker       │
             │   │          │
             │   ▼          │
             │ ShopSphere   │
             └──────┬───────┘
                    │
                    ▼
              PostgreSQL

This gives us a relatively inexpensive and easy-to-understand production deployment while you're learning AWS.

But there's an important architecture decision

For production, I don't recommend running PostgreSQL inside the same EC2 instance.

Eventually we should have:

Internet
   │
   ▼
EC2
ShopSphere
   │
   │ private network
   ▼
AWS RDS PostgreSQL

However, because you want to control AWS costs, we can initially deploy in stages:

Stage 1

EC2
 ├── Docker
 ├── ShopSphere
 └── PostgreSQL

Stage 2

EC2
 └── ShopSphere
       │
       ▼
    RDS PostgreSQL

Stage 3

Application Load Balancer
          │
          ▼
       EC2/ECS
          │
          ▼
      RDS PostgreSQL

And eventually, if ShopSphere grows:

                    ALB
                     │
          ┌──────────┴──────────┐
          ▼                     ▼
      ShopSphere            ShopSphere
       Container             Container
          │                     │
          └──────────┬──────────┘
                     ▼
                  RDS
                     │
              ┌──────┴──────┐
              ▼             ▼
             S3           Kafka/SQS
I suggest our immediate next task

Create the AWS EC2 deployment pipeline from GHCR.

We'll do it carefully, one step at a time:

Create EC2 instance
Configure security group
Install Docker
Authenticate EC2 with GHCR
Pull the ShopSphere image
Configure environment variables securely
Start ShopSphere
Configure PostgreSQL
Test the public API
Then automate deployment from GitHub Actions

I would not put AWS credentials directly into the Docker image or repository. We'll establish the correct secret-management approach as part of this deployment.

You're now at a very good point: GitHub → CI → GHCR is working, so the next failure, if any, will be isolated to the AWS deployment layer rather than the application/CI layer.

--- 

Rough Work

onst token = localStorage.getItem('shopsphere_access_token');

const payload = JSON.parse(
  atob(token.split('.')[1])
);

console.log(payload);
console.log('Issued:', new Date(payload.iat * 1000));
console.log('Expires:', new Date(payload.exp * 1000));
console.log('Now:', new Date());